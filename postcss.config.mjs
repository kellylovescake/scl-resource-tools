/**
 * postcss.config.mjs
 *
 * PostCSS processes CSS files for Tailwind and autoprefixer.
 * This is boilerplate required by Tailwind CSS — do not modify unless
 * you know what you are changing and why.
 */

const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
